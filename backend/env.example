# Backend Environment Variables

# Local development
PORT=3003
NODE_ENV=development

# Firebase emulators (Docker Compose sets these automatically)
# USE_FIRESTORE_EMULATOR=true

# Production (Vercel) — required for live Firebase
# GCLOUD_PROJECT=bp-furever
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
